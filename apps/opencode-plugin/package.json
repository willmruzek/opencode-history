{
  "name": "@oc-hist/opencode-plugin",
  "displayName": "OpenCode History Viewer",
  "description": "Minimal OpenCode plugin for validating hooks",
  "version": "0.1.0",
  "publisher": "opencode",
  "type": "module",
  "main": "./dist/plugin.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -w -p tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {}
}
